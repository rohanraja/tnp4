<%= form_for @link, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :pageno, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :pageno, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :date, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :date, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :html, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :html, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :date_added, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :date_added, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                links_path, :class => 'btn' %>
  </div>
<% end %>
